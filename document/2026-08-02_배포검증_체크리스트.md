# 배포 검증 체크리스트

> Azure Container Apps에 배포된 사이트를 검증하는 재사용 가능한 체크리스트.
> 다음 배포 때는 §3 시나리오 표만 복사해서 결과 칸을 다시 채우면 된다.

## 1. 환경 정보

| 항목 | 값 |
|---|---|
| 배포 URL | https://frontend.agreeableisland-659b2709.koreacentral.azurecontainerapps.io |
| 리소스 그룹 | `mins-shopping-rg` (koreacentral) |
| 컨테이너 앱 | `frontend` (external ingress) / `backend` (**internal** ingress) |
| DB | `mins-shopping-pg` (Azure PostgreSQL Flexible, 단일 인스턴스) |
| 검증일 | 2026-08-02 (1회차 실행 완료) |
| 배포 커밋 | `86f4b51` 기준 추정 (`b322469`는 docs-only 미push) |
| 번들 해시 | `index-DjSvkt_H.js` / `index-D-MpCMl-.css` (835KB) |

### 1회차 실행 결과 요약 (2026-08-02)

**결론: 전 기능 정상. DEF-1(결제 불가)은 같은 날 수정·재배포·재검증 완료.**

| Phase | 결과 |
|---|---|
| P0 인프라·카탈로그·인증·주문 | ✅ 전부 통과 |
| P0 **결제** | ❌ 최초 BLOCKED → ✅ **수정 후 재검증 통과** (§DEF-1) |
| P1 리뷰·마이페이지·관리자 | ✅ 통과 |
| P2 UI·반응형·콘솔 | ✅ 통과 |

발견된 결함 12건은 §9 참조. **DEF-1~10·12 해결 완료, 남은 것은 DEF-11(P2) 하나**다.
(DEF-11은 DEF-5 재검증 중 새로 발견 — 업로드 Blob의 Content-Type 문제로, `<img>` 렌더는 정상이라 낮은 우선순위)

### DEF-1 수정 이력 (2026-08-02, 같은 날 처리 완료)

1. `gh secret set VITE_TOSS_CLIENT_KEY` 로 GitHub Secret 등록
2. 미푸시 커밋 `b322469` push → CI 재빌드 트리거 (run `30725726805`, 3개 job 전부 success)
3. 새 리비전 `frontend--0000011` 활성화, 번들 `index-DjSvkt_H.js` → **`index-bCl5NDZ1.js`** 로 교체
4. 새 번들에 `test_ck_…` 키 존재 확인. 동시에 "결제 키가 설정되지 않았습니다" 가드 문구가 **번들에서 사라짐** — Vite가 키를 상수로 치환하면서 `if (!KEY)` 분기를 dead code로 제거했기 때문
5. **E2E 재검증 통과**: 주문 #3 생성 → 가상계좌(농협) 선택 → 약관 동의 → 확인 → 상태 `WAITING_FOR_DEPOSIT`, 계좌번호 `X9024352970241`, 입금기한 2026-08-09 발급. "입금 확인하기" → "아직 입금이 확인되지 않았습니다"(미입금이므로 정상)
   → **프론트 키와 백엔드 시크릿 키가 모두 정상이고, 같은 상점의 키 쌍임이 확인됨**
6. 재발 방지: `.github/workflows/ci.yml` `deploy` job 맨 앞에 `Verify deploy secrets` 스텝 추가

> ⚠️ **캐시 주의**: 재배포 직후 브라우저가 옛 `index.html`을 캐시해 구버전 번들을 계속 로드했다. 검증 시 `fetch('/index.html', {cache:'no-store'})`로 **서버가 실제로 주는 번들 이름**을 확인할 것. (nginx가 `index.html`에 no-cache 헤더를 주지 않는 점은 개선 여지 → DEF-10)

**FQDN 조회 명령** (저장소에 하드코딩돼 있지 않음):
```bash
az containerapp show --name frontend --resource-group mins-shopping-rg \
  --query properties.configuration.ingress.fqdn
```

## 2. 사전 준비

### 2-1. 계정 (prod에도 `DevDataInitializer`가 실행되어 시드가 들어가 있음)

| 구분 | 이메일 | 비밀번호 |
|---|---|---|
| 관리자 | `admin@shop.com` | `admin1234` |
| 일반 | `test@shop.com` | `test1234` |
| 일반(예비) | `user01@shop.com` ~ `user10@shop.com` | `user1234` |

시드 상품 18개(일반 8 + 정육 10), 태그 = 채소/과일/꾸러미/베이커리/정육.
**주문·리뷰·장바구니 시드는 없다** — 테스트 중에 직접 만들어야 한다.

### 2-2. 테스트 데이터 규칙

운영 DB가 하나뿐이라 테스트 데이터가 그대로 남는다(정리하지 않기로 결정). 오염을 최소화하기 위해:

| 구분 | 값 |
|---|---|
| 주문용 상품 | **충남 금산 당근** (8,500원 / 재고 50) — 재고 여유가 가장 큼 |
| 배송지 받는분 | `테스트 2026-08-02` |
| 관리자 신규 상품 | `TEST-20260802-삭제예정` (접두사로 시드와 구분, 끝나면 삭제) |

### 2-3. 절대 실행 금지 (운영 DB 파괴)

- ❌ 마이페이지 → 설정 → **회원 탈퇴** (시드 계정 소멸)
- ❌ 마이페이지 → 설정 → **비밀번호 변경** (다음 세션 로그인 불가)
- ❌ 관리자 → **시드 상품 18개 삭제/가격 수정** (`TEST-` 접두사 상품만 삭제)
- ⚠️ 리뷰는 한 상품당 1인 1회 — 이미 있으면 수정/삭제 시나리오로 우회

### 2-4. 인프라 제약 (테스트 설계에 직접 영향)

1. **min-replicas 0** (frontend/backend 둘 다) → 유휴 후 첫 요청은 콜드 스타트. 워밍업이 반드시 맨 앞.
2. **backend는 internal ingress** → 외부에서 직접 호출 불가. 모든 검증은 `https://<frontend>/api/...` 경유.
3. **`/actuator/health`는 nginx가 프록시하지 않음** → 외부 헬스체크 불가. `GET /api/products`를 헬스 프로브로 사용.
4. **세션은 인메모리** → 컨테이너 재기동 시 로그인이 풀린다. 테스트 중 갑자기 401이 뜨면 이걸 의심.
5. 테스트 중 **5분 이상 유휴 금지** — 사람 입력을 기다리는 구간 전후로 `fetch('/api/products?size=1')` 핑.

## 3. 시나리오

결과 기호: ✅PASS / ❌FAIL / ⚠️PARTIAL / ⏭SKIP / 🔒BLOCKED

**중단 기준**: S0-2(백엔드 기동) 또는 S2-2(로그인)가 FAIL이면 이후 전부 BLOCKED 처리하고 중단.

### Phase 0 — 워밍업 & 인프라 【P0】

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S0-1 | P0 | 프론트 컨테이너 기동 | 홈페이지 200 (nginx라 2~5초) | ⬜ | |
| S0-2 | P0 | 백엔드 콜드스타트 | `/api/products` 200, **소요 __초** (40~120초 예상) | ⬜ | 이 값이 이후 타임아웃 기준 |
| S0-3 | P0 | 시드 무결성 | `totalElements=18`, 모든 `imageUrl`이 `/uploads/` 시작, 정육 10개 | ⬜ | |
| S0-4 | P0 | 배포 커밋 식별 | `/assets/index-*.js` 해시 기록 | ⬜ | 다음 배포 판별용 |

**S0-2 워밍업 스크립트** (MCP 툴 타임아웃 회피를 위해 JS 안에서 폴링):
```js
async () => {
  const t0 = Date.now();
  let last;
  for (let i = 0; i < 36; i++) {
    try {
      const r = await fetch('/api/products?size=1', { credentials: 'include' });
      if (r.ok) return { ok: true, ms: Date.now() - t0 };
      last = r.status;
    } catch (e) { last = 'network:' + e.message; }
    await new Promise(s => setTimeout(s, 5000));
  }
  return { ok: false, ms: Date.now() - t0, last };
}
```

### Phase 1 — 공개 카탈로그 (비로그인) 【P0/P1】

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S1-1 | P0 | 홈 렌더링 + 베스트 | 히어로 이미지, 베스트 상품, 배지 노출. **리뷰/주문 시드가 없으므로 초기엔 `LATEST`가 정상** | ✅ | DEF-4로 이어짐 |
| S1-2 | P0 | 시드 이미지 로딩 | 깨진 이미지 0건 | ⬜ | 아래 스크립트 |
| S1-3 | P0 | 목록 + 페이징 | 1p 10개 / 2p 8개, 마지막에서 "다음" disabled | ⬜ | |
| S1-4 | P0 | 검색 | `한우` → 3건 (등심/불고기감/국거리) | ⬜ | |
| S1-5 | P1 | 카테고리·평점 필터 | 정육 10건, 채소 3건. **리뷰 없으니 평점 4점+ 는 0건이 정상** | ⬜ | S6 이후 재확인 |
| S1-6 | P1 | 상품 상세 + 추천 | `재고 50개` ← **재고 스냅샷 #1**. 같은 태그 추천 노출. 비로그인은 리뷰 폼 미노출 | ⬜ | |
| S1-7 | P0 | 비로그인 접근 차단 | `/cart` `/orders` `/mypage` `/admin` → `/login` 리다이렉트 | ⬜ | |

**S1-2 깨진 이미지 검출**:
```js
() => [...document.images]
  .map(i => ({ src: i.currentSrc, ok: i.complete && i.naturalWidth > 0 }))
  .filter(x => !x.ok)
```
> 실패 시: 응답 Content-Type이 `text/html`이면 nginx `try_files`가 index.html로 fallback한 것 = 이미지 파일이 빌드에 안 들어감.

### Phase 2 — 인증 【P0】

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S2-1 | P0 | 로그인 실패 | 에러 메시지, 이동 없음, 무한 리다이렉트 없음 | ⬜ | 인터셉터가 `/auth/`는 제외 |
| S2-2 | P0 | 로그인 성공 | Navbar에 마이페이지/로그아웃, **관리자 링크는 없어야 함** | ⬜ | |
| S2-3 | P0 | 세션 지속 | 새로고침 후 `GET /api/auth/me` 200 | ⬜ | 401이면 세션 인메모리 문제 |

### Phase 3 — 장바구니 → 주문 생성 【P0】

> ⚠️ **재고 차감은 결제가 아니라 주문 생성 시점**에 일어난다 (`OrderService.createOrder()` → `decreaseStock()`).

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S3-1 | P0 | 장바구니 담기 | 토스트, Navbar 배지 갱신 | ⬜ | 당근 2개 |
| S3-2 | P1 | CartDrawer | 사이드바 열림, 소계 정확 | ⬜ | |
| S3-3 | P1 | 수량 변경/삭제 | 소계 즉시 재계산 | ⬜ | |
| S3-4 | P2 | 배송비 경계값 | 40,000 미만 → 3,500원 / 이상 → 무료 | ⬜ | |
| S3-5 | P0 | 주문 생성 | 201 → `/orders/{id}`, 상태 `주문완료`, 장바구니 비워짐 | ⬜ | **재고 스냅샷 #2: 50→48** |

> **S3-5에서 같이 확인할 것**: CartPage 버튼은 배송비 포함 금액(20,500)을 표시하는데 서버는 상품가 합계(17,000)로 주문을 만든다 → 금액 불일치 (§6 예상 결함)

**주문은 3건 만든다**: A(가상계좌용) / B(카드용) / C(결제취소용)

### Phase 4 — 결제 【P0】 ★ 최우선

**전략: 가상계좌를 먼저 돌려 두 토스 키를 모두 검증하고, 그 다음 카드로 PAID까지 간다.**

| | 프론트 키 검증 | 백엔드 키 검증 | 자동화 | PAID 도달 |
|---|---|---|---|---|
| 가상계좌 | ✅ | ✅ | **✅ 100%** | ❌ (입금대기) |
| 카드 | ✅ | ✅ | ⚠️ 카드사 보안모듈 구간 불가 | ✅ |

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S4-1 | P0 | 가상계좌 (주문 A) | 결제창 열림 → 상태 `입금대기` → 계좌 안내 박스 → "입금 확인하기" → "아직 입금이 확인되지 않았습니다"(정상) | ⬜ | 키 2종 판별 |
| S4-3 | P0 | 카드 (주문 B) | 상태 `결제완료`(PAID), 결제수단 UI 사라짐 | ⬜ | 반자동 |
| S4-5 | P2 | 결제 취소 (주문 C) | `USER_CANCEL`은 alert 없이 조용히 닫힘, 주문은 ORDERED 유지 | ⬜ | |

#### 결제 실패 → 원인 즉시 판별 (핵심 진단표)

| 증상 | 원인 |
|---|---|
| `alert("결제 키가 설정되지 않았습니다...")` — 결제창 자체가 안 열림 | **`VITE_TOSS_CLIENT_KEY` 누락** (GitHub Secret → Docker build-arg) |
| 결제창은 뜨는데 주문이 **"취소됨"** | **`TOSS_SECRET_KEY` 누락/오류** — 토스 401 → `ConfirmResult.failed()` → CANCELED + 재고 복구. **HTTP는 200이라 에러로 안 보인다** |
| "결제 승인 처리 중 문제가 발생했습니다" | `POST /orders/{id}/pay`가 4xx/5xx (금액 불일치 or 백엔드 예외) |
| 토스가 "잘못된 클라이언트 키" 표시 | 두 키가 서로 다른 상점의 키 (짝 불일치) |

#### 카드 결제 절차 (사람 개입 지점)

1. **[자동]** `/orders/{B}` → 카드 라디오 → "결제하기" → `browser_tabs(list)`로 새 탭/팝업 여부 실측 → 스냅샷
2. **[분기]**
   - (a) 결제창에 "결제하기" 버튼만 → 그대로 클릭, 완전 자동
   - (b) 카드번호 입력 화면 → 일반 `<input>`이면 자동 입력 시도. **보안 키패드(canvas/iframe)면 자동화 불가 → 사람에게 hand-off**
   - (c) 결제창이 아예 안 뜸 → 위 진단표로 판정 후 §S4-4 우회로
3. **[자동]** 원래 탭으로 복귀 → `/payments/success` → `/orders/{B}` → 상태 `결제완료` 확인

> hand-off 대기 중에는 **백엔드 재수면 방지 핑**을 걸 것.

#### S4-4. PAID 도달 실패 시 우회로 (S6 언블록 전용)

관리자 → 주문관리 → 주문 B를 `결제완료`로 변경 (`ORDERED → PAID` 전이는 허용됨).
⚠️ **이건 결제 검증이 아니다.** S4-3을 🔒BLOCKED로 기록하고, 결제 승인 검증은 S4-1 결과로만 판정한다.

### Phase 5 — 결제 후 상태·재고 【P0】

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S5-1 | P0 | 재고 정합성 | `50 - A수량 - B수량 - C수량 + 취소분` | ⬜ | 재고 스냅샷 #3 |
| S5-2 | P0 | 주문 목록 | A(입금대기)/B(결제완료)/C(주문완료), 최신순 | ⬜ | |
| S5-3 | P1 | 타인 주문 차단 | `fetch('/api/orders/1')` → **403** | ⬜ | 200이면 심각한 인가 결함 |

### Phase 6 — 리뷰 【P1】 ※ PAID 선행 필수

> ⚠️ `ReviewService.createReview()`가 **구매 이력(PAID/SHIPPING/DELIVERED)을 검사**한다. 주문 B가 PAID여야 실행 가능.

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S6-1 | P1 | 리뷰 작성 | 별점 5 + 내용 → 목록 즉시 추가, 평균 별점·**키워드 배지** 생성 | ⬜ | 403이면 PAID 아님 |
| S6-2 | P1 | 리뷰 수정/삭제 | 인라인 편집, 삭제 후 `리뷰 (0)` | ⬜ | **삭제는 S6-3 이후** |
| S6-3 | P1 | 카탈로그 반영 | 평점 4점+ 필터에 1건. 홈 배지 `LATEST` → **`REVIEW_BEST`** | ⬜ | S1-1/S1-5와 비교 |
| S6-4 | P2 | 타인 리뷰 차단 | API로 `PUT` → 403 | ⬜ | |

### Phase 7 — 마이페이지 【P1】

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S7-1 | P1 | 주문 탭 | S5-2와 동일 3건 | ⬜ | |
| S7-2 | P1 | 배송지 CRUD | 추가/수정/기본 지정. **기본은 항상 1개만** | ⬜ | Daum 팝업 |
| S7-3 | P1 | 이름 변경 | Navbar 표시명 갱신 → **반드시 원복** | ⬜ | 비번변경·탈퇴 금지 |

> **S7-2 연계**: `/cart`의 "저장된 배송지" 모달에 기본 배송지가 맨 위로 오는지 (S3-5에서 "없습니다"였던 것과 대비)

### Phase 8 — 관리자 【P1】

로그아웃 → `admin@shop.com` 로그인. Navbar에 `관리자` 링크 노출 확인.

| ID | 우선 | 시나리오 | 기대 결과 | 결과 | 비고 |
|---|---|---|---|---|---|
| S8-1 | P1 | 상품 CRUD | `TEST-20260802-삭제예정` 등록→수정(재고 3)→삭제. 재고 5면 **`마감임박` 배지** | ⬜ | 끝나고 반드시 삭제 |
| S8-2 | P1 | 이미지 업로드 API | 200 + Blob URL | ⬜ | **UI 없음 → JS로 직접 호출** |
| S8-3 | P1 | 주문 상태 관리 | 필터 동작, B를 `배송중`→`배송완료`. 역전이 거부 | ⬜ | **가상계좌 주문 select 깨짐 확인** |
| S8-4 | P0 | 권한 분리 | USER로 `/admin` → 리다이렉트, `fetch('/api/admin/orders')` → **403** | ⬜ | |

**S8-2 업로드 호출** (관리자 UI에 업로드 버튼이 없어서 직접 호출):
```js
async () => {
  const c = document.createElement('canvas'); c.width = c.height = 1;
  const blob = await new Promise(r => c.toBlob(r, 'image/png'));
  const fd = new FormData();
  fd.append('file', new File([blob], 'qa-20260802.png', { type: 'image/png' }));
  const res = await fetch('/api/admin/upload', { method: 'POST', body: fd, credentials: 'include' });
  return { status: res.status, body: await res.text() };
}
```
판정: `503` → `AZURE_STORAGE_CONNECTION_STRING` 미주입 / `403` → admin 세션 아님 / `500` → Blob 컨테이너 없음

### Phase 9 — UI 디테일 / 엣지 【P2】

| ID | 시나리오 | 기대 결과 | 결과 |
|---|---|---|---|
| S9-1 | 정적 페이지 | `/story`, `/about` 렌더링 | ⬜ |
| S9-2 | 품절 배지 | 재고 0 → 흑백 + `품절` 배지 | ⬜ |
| S9-3 | 재고 초과 담기 | 수량 스테퍼가 재고 수에서 멈춤 | ⬜ |
| S9-4 | 반응형 | `resize(390,844)` → 1단 레이아웃, 햄버거 메뉴 | ⬜ |
| S9-5 | 콘솔 청결도 | **비로그인 `/api/auth/me` 401은 정상**(기결론). 그 외 목록화 | ⬜ |
| S9-6 | 네트워크 감사 | 4xx/5xx 전량 목록화 | ⬜ |
| S9-7 | 404 | 없는 경로 → NotFoundPage | ⬜ |

## 4. 브라우저 자동화 걸림돌 & 대응

| 걸림돌 | 대응 |
|---|---|
| **콜드 스타트** (min-replicas 0 × 2) | S0-2 폴링을 맨 앞에 고정. MCP 툴 타임아웃을 피하려 **JS 안에서 폴링** |
| **재수면** | 5분 이상 유휴 구간 전후로 `/api/products?size=1` 핑 |
| **세션 소멸** | 갑자기 401 → 재로그인 후 해당 시나리오만 재실행, 체크리스트에 기록 |
| **Daum 우편번호 팝업** | `window.open` 새 창 → `browser_tabs`로 전환. 불안정하면 **S7-2에서 배송지를 먼저 저장한 뒤 CartPage의 "저장된 배송지" 모달로 우회** |
| **토스 결제창** | 새 탭/팝업/리다이렉트 중 무엇인지 **클릭 후 실측**하고 분기. 보안 키패드는 사람 hand-off |
| **승인 후 리다이렉트 놓침** | `PaymentSuccessPage`가 `useRef`로 중복 방지 → 새로고침해도 confirm 재요청 안 됨. `/orders/{id}`로 직접 이동해 상태 확인 |
| **confirm/alert** | `browser_handle_dialog` 항상 준비 (처리 안 하면 이후 조작 전부 블로킹) |
| **로케이터** | `data-testid`가 거의 없음 → `aria-label`("상품 검색", "장바구니, 담긴 상품 N개", "품절된 상품") / 텍스트 기반 |

## 5. 실패 시 원인 切り分け — 4계층 진단

```
증상 발생
 ├─ ① 화면이 안 뜬다
 │    ├─ JS 번들 404        → 프론트 이미지 빌드 실패 (레이어: 빌드/CD)
 │    └─ JS 에러            → 프론트 코드 버그 (레이어: 프론트)
 │
 ├─ ② API 실패 → 상태코드로 분기
 │    ├─ Content-Type이 text/html (JSON 기대) → nginx가 /api를 프록시 안 하고 SPA fallback
 │    │                                        (레이어: nginx / BACKEND_URL 미주입 / 구버전 이미지)
 │    ├─ 502·504  → nginx는 살아있고 백엔드 도달 실패 (콜드스타트 or 내부 DNS)
 │    ├─ 401      → /api/auth/* 면 정상. 그 외면 세션 소실 (컨테이너 재기동 의심)
 │    ├─ 403      → 인가. 의도된 동작일 수도 (S5-3, S8-4)
 │    ├─ 500      → /api/products가 되면 DB 정상 → 로직 문제
 │    │             /api/products도 500 → DB 연결/스키마 문제
 │    └─ 200인데 데이터 이상 → 시드 미실행 or 이전 테스트 잔여물
 │
 └─ ③ 결제 실패 → §Phase 4 진단표
```

**레이어 확정용 3-핑** (증상이 애매할 때 한 번에):
```js
async () => {
  const probe = async (u, o) => {
    try { const r = await fetch(u, o); return { u, s: r.status, ct: r.headers.get('content-type') }; }
    catch (e) { return { u, err: e.message }; }
  };
  return [
    await probe('/index.html'),                               // ① nginx 정적
    await probe('/api/products?size=1'),                      // ② nginx→백엔드→DB
    await probe('/api/auth/me', { credentials: 'include' }),  // ③ 세션
  ];
}
```
- ①만 성공 → nginx 프록시 문제
- ①② 성공 / ③ 401 → 세션 문제
- 전부 성공인데 화면이 깨짐 → 프론트 렌더링 문제

## 6. 예상 결함 (코드 리딩 단계에서 미리 등록 — 확인만 하면 됨)

| 심각도 | 결함 | 확인 |
|---|---|---|
| P1 | `AdminPage.ORDER_STATUSES`에 `WAITING_FOR_DEPOSIT` 누락 → 가상계좌 주문의 상태 select가 매칭 option 없이 렌더 + React 콘솔 경고 | S8-3 |
| P1 | 관리자 이미지 업로드 **UI 부재** — `frontend/src/api/admin.js`의 `uploadImage()`가 어디서도 import 안 되는 dead code. 백엔드/Blob은 있는데 쓸 방법이 없음 | S8-2 |
| P1 | CartPage 버튼은 배송비 포함 금액을 표시하지만 서버 주문 금액은 상품가 합계 → 불일치 | S3-5 |
| P2 | 세션이 인메모리 → scale-to-zero 환경에서 컨테이너 재기동 시 로그인 소실 | S2-3 |
| UX | `TOSS_SECRET_KEY` 실패가 사용자에게 "취소됨"으로만 보이고 이유를 알 수 없음 | S4-1 |

### DEF-6 상세 — 조사 범위가 넓어진 사례와 수정 (커밋 `bd5cae8`)

**최초 증상**(관리자 화면)은 빙산의 일각이었다. `STATUS_LABEL` 상수가 **4개 파일에 복사**돼 있었고, 가상계좌 기능 추가 시 `OrderDetailPage` 하나만 갱신된 게 원인이다.

| 화면 | 수정 전 | 수정 후 |
|---|---|---|
| `/orders/:id` 주문 상세 | 입금대기 ✅ | 입금대기 ✅ |
| `/orders` 주문 내역 | `WAITING_FOR_DEPOSIT` ❌ | 입금대기 |
| `/mypage` 주문 탭 | `WAITING_FOR_DEPOSIT` ❌ | 입금대기 |
| `/admin` 주문 관리 | 배지 `WAITING_FOR_DEPOSIT` + 드롭다운 **"주문완료"** ❌ | 배지·드롭다운 모두 입금대기 |

관리자 화면이 특히 위험했던 이유: `<select value="WAITING_FOR_DEPOSIT">`에 매칭되는 `<option>`이 없으면 브라우저가 **첫 번째 option(`ORDERED`)을 선택된 것처럼 표시**한다. 빈 값이 아니라 **틀린 값을 그럴듯하게** 보여주므로, 관리자가 미입금 주문을 결제된 것으로 오인할 수 있었다.

**수정 방식** — AdminPage에 한 줄 추가하는 대신 상수를 추출했다(그렇게 하면 4번째 복사본이 되고 다음 상태 추가 때 같은 사고가 반복된다):

- `frontend/src/utils/orderStatus.js` 신규 — 라벨/관리자 지정가능 상태/옵션 생성 일원화
- `adminStatusOptions(current)` — 현재 상태가 수동 지정 불가한 값이어도 option에 **포함하되 `disabled`** 처리. 입금대기는 토스 결제 흐름이 정하는 값이라 관리자가 임의로 만들 수 없지만, **표시는 정확해야** 하기 때문
- 4개 파일의 중복 정의 제거
- `orderStatus.test.js` 11개 — **"백엔드 enum 전체에 한글 라벨이 있는가"** 를 테스트로 고정. 백엔드에 상태가 추가되면 이 테스트가 라벨 누락을 잡는다 (재발 방지의 핵심)

**배포 후 재검증 완료** (같은 날, 캐시 우회 확인 포함):
- 번들 `index-DcWjj621.js`로 교체 확인. ⚠️ 재확인 중 **캐시가 DEF-1 이전 원본 번들(`index-DjSvkt_H.js`)을 서빙하는 것을 실제로 목격** — DEF-10이 실전에서 재현됨. `?_=999` 캐시버스팅 쿼리로 우회
- `/orders`, `/mypage`: 주문 #3(WAITING_FOR_DEPOSIT)이 **"입금대기"**로 정상 표시
- `/admin` 주문 관리: 배지 "입금대기" + 드롭다운도 **"입금대기"로 선택 표시**(disabled), 옵션 목록에 6개 상태 모두 노출, 필터 드롭다운에도 "입금대기" 포함
- 증거: `admin-waiting-deposit-fixed.png` (수정 전 `admin-waiting-deposit-defect.png`와 대비)

**DEF-2 배포 후 재검증** (같은 날): 번들 교체 확인 후 API로 두 경계 재확인 — 8,500원(`totalPrice:12000, shippingFee:3500`), 47,500원(`totalPrice:47500, shippingFee:0`, 주문 #5). `/orders/4` 화면에서 "충남 금산 당근 × 1개 8,500원 / 배송비 3,500원 / 합계 12,000원" 정확히 표시 확인 (`def2-order-detail-shipping.png`).

**DEF-3 배포 후 재검증**: `user01@shop.com`(구매 이력 없는 신규 계정)으로 로그인 → 상품 상세에서 리뷰 등록 클릭 → 63ms 만에 "구매자만 리뷰를 작성할 수 있습니다." 토스트 정확히 표시 (`def3-recheck.png`).

### DEF-10 상세 — nginx 캐시 헤더 (커밋 `58526f9`)

**원인**: `frontend/nginx.conf`의 `location /` (SPA fallback 포함)에 캐시 헤더가 전혀 없어 브라우저 기본 정책에 맡겨져 있었다. `/assets/*.js|css`는 Vite가 콘텐츠 해시를 파일명에 붙이므로 내용이 바뀌면 파일명도 바뀌어 영구 캐시해도 안전한데, `index.html`은 해시 없는 고정 파일명이라 정반대로 가야 했다.

**수정**:
```nginx
location /assets/ {
    add_header Cache-Control "public, max-age=31536000, immutable";
}
location = /index.html {
    add_header Cache-Control "no-cache";  # 저장은 하되 매번 재검증(ETag로 304 가능)
}
```

**검증 방법** (Azure 배포 전, 로컬에서 실제 이미지로):
1. `envsubst`로 템플릿 치환 후 `docker run nginx:alpine nginx -t` — 문법 검증
2. `docker build`로 실제 프론트 이미지 생성 (CRLF 이슈로 커스텀 entrypoint 대신 `docker-entrypoint.d/20-envsubst-on-templates.sh` 직접 실행 후 `nginx -g 'daemon off;'`로 우회 실행)
3. `curl -I`로 세 경로 확인 — `index.html`·`/orders`(SPA 딥링크, try_files 내부 리다이렉트 경유)·`/assets/*.js` 모두 의도한 `Cache-Control` 확인

### DEF-2 상세 — 백엔드에 배송비 도입 (커밋 `002c1cb`)

**원인**: 배송비 개념 자체가 백엔드에 없었다. `Order` 엔티티에 필드 없음, `OrderService.createOrder()`는 상품가 합계만 계산, `pay()`의 결제 금액 검증도 상품가만 비교. 프론트(`CartPage.jsx`)만 독자적으로 배송비를 계산해 "N원 결제하기" 버튼에 표시했지만, 실제 `POST /orders` 요청에는 그 값을 싣지 않았다.

**수정**:
- `Order.java`에 `FREE_SHIPPING_THRESHOLD`(40,000원)/`SHIPPING_FEE`(3,500원) 상수, `shippingFee` 필드(주문 시점 스냅샷), `applyShippingFee()` — 상품을 모두 담은 뒤(장바구니 전체 합계 기준으로) 호출
- `shippingFee` 컬럼은 **`columnDefinition="integer default 0"`으로 추가** — 기본값 없이 `nullable=false`만 붙이면 기존 row가 있는 테이블에 DDL 자체가 거부된다(과거 `Product.viewCount`와 같은 함정). 로컬 DB(기존 주문 22건)에 실제 재배포해 DDL이 깨지지 않고 전부 `shipping_fee=0`으로 채워지는 것까지 확인
- `pay()`의 결제 금액 검증과 프론트의 토스 결제 요청 금액(`amount: order.totalPrice`)은 **코드 변경 없이 자동으로 정합성이 맞음** — `totalPrice`가 이제 배송비를 포함하기 때문
- `OrderDetailPage.jsx`에 "배송비" 줄 추가 — "상품금액 + 배송비 = 합계" 형태로 표시

**검증**: TDD로 테스트 먼저 작성(`OrderTest.java` 경계값 포함 4개, `OrderServiceTest.java` end-to-end 2개) → 전체 백엔드 테스트 통과 → 로컬 Docker로 실제 재배포(기존 22건 주문 DB) → API로 두 경계 직접 확인:
- 8,500원(4만원 미만) → `totalPrice: 12000, shippingFee: 3500`
- 47,500원(4만원 이상) → `totalPrice: 47500, shippingFee: 0`

### DEF-3 상세 — 조사 결과 별도 수정 불필요

`ProductDetailPage.jsx`의 `handleReviewSubmit`은 이미 `catch (err) { showMessage(err.response?.data?.message || '리뷰 작성에 실패했습니다') }`로 되어 있었다(커밋 `0b426b4`, 2026-07-27 — 최초 배포 기준선 `86f4b51`보다 먼저). `Toast` 컴포넌트, `CartContext`의 `showMessage` 배선도 모두 정상.

최초 "화면에 아무 표시도 없음" 관찰은 **DEF-10(캐시)의 부작용**이었을 가능성이 유력하다 — 그 시점 브라우저가 로드한 번들이 이 Toast 수정보다도 더 오래된 버전이었을 것. DEF-10을 고친 뒤 `user01@shop.com`(구매 이력 없는 계정)으로 재현 시도했더니, 리뷰 등록 버튼 클릭 63ms 만에 "구매자만 리뷰를 작성할 수 있습니다." 토스트가 정확히 표시됐다. 별도 코드 수정 없이 종결.

### DEF-4 상세 — 베스트 상품 선정을 "배타적 전환"에서 "채우기"로 (커밋 `59744a4`)

**원인**: `ProductService.findBestProducts()`의 3단계 폴백(REVIEW_BEST→SALES→LATEST)이 "앞 단계가 하나라도 결과를 내면 그걸로 전량 확정"하는 배타적 전환이었다. 전환 임계값이 코드에도 테스트에도 없었고, 오히려 "판매 실적 상품 1개만 있어도 SALES 전환"이 기존 테스트에 정상 명세로 박혀 있었다.

**수정**: 각 단계가 앞 단계가 요청 개수(size=5)를 못 채운 만큼만 다음 단계로 넘어가 REVIEW_BEST+SALES+LATEST를 섞어서라도 최대한 채운다. `LinkedHashMap`(상품 id 키)으로 여러 단계 후보에 겹치는 상품의 중복을 제거하면서 먼저 뽑힌 우선순위를 유지. 화면 상단 배지는 목록 첫 항목의 source를 그대로 쓰므로 "가장 강한 근거가 대표로 노출"되는 기존 의미는 유지된다.

프론트(`HomePage.jsx`)도 방어적으로 손봤다 — 그리드 컨테이너의 회색 배경(gap 경계선 용도)이 빈 칸에 비치는 문제를 막기 위해 데스크톱 열 수를 실제 카드 개수에 맞춤(모바일은 `!important` 규칙이 그대로 2열 강제라 영향 없음).

**검증**: TDD로 기존 3개 테스트에 후속 단계 stub 추가(NPE 방지) + 채우기 동작 자체를 검증하는 신규 테스트 2개(판매1+최신3 채우기, 여러 단계 중복 상품 처리) → 전체 27개 통과. 로컬 Docker 재배포 후 API 확인(로컬은 판매 실적이 이미 다양해 SALES만으로 5개 채워짐).

**배포 후 실전 재검증** (같은 날): 원래 DEF-4가 재현되던 바로 그 데이터(당근만 판매 1건)로 확인 — `/api/products/best` 응답이 `[당근(SALES), 닭발(LATEST), 국거리(LATEST), 갈비(LATEST), 훈제오리(LATEST)]` 5개로 채워짐. 홈 화면 그리드 4칸 전부 카드로 꽉 참(빈 회색 박스 없음), 부제 "판매량 기준 인기 상품" 정확히 표시. 증거: `def4-homepage-fixed-grid.png` (최초 발견 당시의 `homepage-best-collapsed.png`와 대비).

## 7. 실행 순서 요약

```
S0 워밍업 → S1 공개 → S2 로그인 → [S7-2 배송지 선등록 ← Daum 팝업 우회용]
→ S3 장바구니·주문×3 → S4-1 가상계좌(키 2종 판별) → S4-3 카드(반자동)
→ S5 재고·상태 → S6 리뷰 → S7 마이페이지 → S8 관리자 → S9 UI
```

예상 소요: P0만 40~60분, 전체 90~120분.

## 8. 테스트 데이터 잔여물 (2026-08-02 1회차)

정리하지 않기로 했으므로 **기록만 남긴다**. 다음 세션에서 "리뷰 중복" 등을 만나면 여기를 확인.

| 종류 | ID/값 | 상태 | 비고 |
|---|---|---|---|
| 주문 | **#2** (test@shop.com, 당근×2, 17,000원) | `DELIVERED` | 결제 없이 관리자 PATCH로 전이시킴(§S4-4 우회로). **되돌릴 수 없음** — 역전이 차단 |
| 주문 | **#3** (test@shop.com, 당근×1, 8,500원) | `WAITING_FOR_DEPOSIT` | DEF-1 수정 후 실제 가상계좌 결제로 생성. 농협 `X9024352970241`, 입금기한 2026-08-09. 입금하지 않으면 기한 후 만료됨. **`shippingFee` 컬럼 마이그레이션 이전 주문이라 `shippingFee: 0`으로 남음(정상)** |
| 주문 | **#4** (DEF2재검증, 8,500원) | `ORDERED` | DEF-2 재검증용. `totalPrice: 12000, shippingFee: 3500` — 화면 배송비 줄 표시 확인용 |
| 주문 | **#5** (DEF2재검증2, 47,500원) | `ORDERED` | DEF-2 재검증용. `totalPrice: 47500, shippingFee: 0` — 무료배송 경계 확인용 |
| 리뷰 | **#1** (상품 1 당근, 별점 5, test@shop.com) | 유지 | 같은 계정으로 당근에 리뷰 재작성 시 409 발생 → 수정/삭제로 우회할 것 |
| 배송지 | **#1** (테스트 2026-08-02, 기본 배송지) | 유지 | |
| 상품 | `TEST-20260802-삭제예정` (id 19) | **삭제 완료** | 상품 수 18개로 복귀 확인 |
| Blob | `product-images/858d0867-..._qa-20260802.png` | 유지 | 1×1 테스트 PNG (S8-2 최초 검증분) |
| Blob | `product-images/{c9dc3542, 5ba2e5a0, 3e689566}-..._def5-test.png` **3건** | 유지 | DEF-5 재검증분 — curl 사전확인 1 + 브라우저 검증 1 + 미리보기 재확인 1. Blob 삭제 API를 붙이지 않아 지울 수단이 없어 기록만 남긴다 |

**사전 존재 데이터**: 주문 **#1** (2026-08-01, 홍성 한우 국거리 18,000원, `ORDERED`) — 이번 테스트 이전에 이미 존재. 결제되지 않은 채 남아 있으며, DEF-1(토스 키 누락)이 배포 시점부터 존재했다는 정황 증거.

## 9. 발견된 결함 (2026-08-02 1회차)

| ID | 심각도 | 결함 | 재현 절차 | 레이어 |
|---|---|---|---|---|
| ~~**DEF-1**~~ | ~~P0~~ | ~~**배포 환경에서 결제 완전 불가.**~~ **✅ 2026-08-02 해결 완료** (위 수정 이력 참조) | — | CI/빌드 |
| ~~**DEF-2**~~ | ~~P1~~ | ~~**금액 불일치.**~~ **✅ 2026-08-02 해결 완료** (커밋 `002c1cb`, 아래 상세) | — | 백엔드(배송비 개념 부재) |
| ~~**DEF-3**~~ | ~~P1~~ | ~~**리뷰 실패가 조용히 묻힘.**~~ **✅ 조사 결과 이미 해결돼 있었음** — 프론트 코드(`showMessage(err.response?.data?.message...)`)는 커밋 `0b426b4`(2026-07-27)부터 정상. 최초 관찰은 **DEF-10(캐시)의 증상**이었다 — 그 수정보다 더 오래된 번들이 서빙되고 있었음. DEF-10 수정 후 재검증: 클릭 63ms 만에 "구매자만 리뷰를 작성할 수 있습니다." 토스트 정상 표시 (`def3-recheck.png`) | — | (수정 불필요, 재검증만) |
| ~~**DEF-4**~~ | ~~P1~~ | ~~**홈 베스트 섹션이 판매 발생 즉시 붕괴.**~~ **✅ 2026-08-02 해결 완료** (커밋 `59744a4`, 아래 상세) | — | 백엔드(best 쿼리) |
| ~~**DEF-5**~~ | ~~P1~~ | ~~**관리자 이미지 업로드 UI 부재.**~~ **✅ 2026-08-02 해결 완료** (커밋 `8322007`, 배포 후 실전 재검증까지 통과. 아래 상세) | — | 프론트 |
| **DEF-11** | P2 | **Blob 이미지의 Content-Type이 `application/octet-stream`.** `AzureBlobService.upload()`가 `BlobHttpHeaders`로 Content-Type을 지정하지 않아 업로드된 이미지가 전부 octet-stream으로 서빙된다. `<img>` 태그는 브라우저가 내용을 스니핑해 정상 렌더하므로 **화면은 깨지지 않지만**, URL 직접 열람 시 표시 대신 다운로드가 되고 CDN/이미지 최적화 계층이 이미지로 인식하지 못할 수 있다 | 업로드 후 Blob URL에 `curl -I` → `content-type: application/octet-stream` | 백엔드 |
| ~~**DEF-6**~~ | ~~P1~~ | ~~**입금대기 주문 상태 표기 누락.**~~ **✅ 2026-08-02 해결 완료** (커밋 `bd5cae8`, 아래 상세) | — | 프론트 |
| ~~**DEF-9**~~ | ~~P2~~ | ~~**관리자 주문 목록 페이징이 마지막 페이지를 인식 못함.**~~ **✅ 2026-08-02 해결 완료** (커밋 `c4c8143`, 배포 후 실전 재검증 6/6 통과. 아래 상세). 1회차에 "주문 탭 고유 문제"로 적었으나 실측 결과 **상품 탭도 같은 조건이 복붙돼 함께 깨져 있었음** | — | 프론트 |
| ~~**DEF-10**~~ | ~~P2~~ | ~~**`index.html`에 캐시 무효화 헤더 없음.**~~ **✅ 2026-08-02 해결 완료** (커밋 `58526f9`, 아래 상세). 실전에서 직접 재현됨 — DEF-6 재배포 직후 브라우저가 DEF-1 이전(가장 오래된) 번들을 서빙하는 것을 목격한 뒤 우선순위를 올려 즉시 수정 | — | nginx 설정 |
| ~~**DEF-7**~~ | ~~P2~~ | ~~**시맨틱/접근성.**~~ **✅ 2026-08-02 해결 완료** (커밋 `461dbf2`, 아래 상세). `CLAUDE_CONTEXT.md`의 P2-7(네비게이션 시맨틱 교체)도 같은 커밋으로 함께 종결 | — | 프론트 |
| ~~**DEF-8**~~ | ~~P2~~ | ~~**모바일(390px)에서 "검색" 버튼이 `검/색`으로 세로 분리됨.**~~ **✅ 2026-08-02 해결 완료** (아래 상세). "초기화" 버튼도 같은 원인으로 함께 깨져 있었음 | — | 프론트 CSS |
| ~~**DEF-12**~~ | ~~P2~~ | ~~**상품 상세의 "추천 상품" 카드 4개가 아직 가짜 링크.**~~ **✅ 2026-08-02 해결 완료** (커밋 `db5f396`, 아래 상세). 같은 자리에서 **DEF-4와 같은 원인의 회색 빈 칸 결함도 함께 발견·수정**. 이하 원래 기록: **상품 상세의 "추천 상품" 카드 4개가 아직 가짜 링크.** DEF-7에서 놓친 잔여분 — `ProductDetailPage.jsx:384`가 `ProductCard` 컴포넌트를 재사용하지 않고 그 자리에 `<div onClick={() => navigate(...)}>`로 직접 그려놨다. 컴포넌트를 고쳐도 **복제본은 안 고쳐진다**는 중복 코드의 전형적 비용. 증상은 DEF-7과 동일(Tab 도달 불가 / Enter 무반응 / 새 탭 열기 불가 / 스크린리더 미인식) | `/products/{id}` 하단 "추천 상품"에서 Tab 키로 카드 도달 시도 → 건너뜀 | 프론트 |

### DEF-5 상세 — 죽어 있던 업로드 경로를 화면에 연결

**원인**: 백엔드는 완성돼 있었다. `UploadController`(`POST /api/admin/upload`) → `AzureBlobService` → Azure Blob까지 전부 정상이고, S8-2에서 JS로 직접 호출했을 때 200 + 공개 URL을 받았다. 빠진 건 **화면 하나**였다 — AdminPage 상품 폼에 `<input type="file">`이 없어서 `frontend/src/api/admin.js`의 `uploadImage()`가 어디서도 import되지 않는 dead code로 남아 있었다. 관리자는 이미지 URL을 손으로 타이핑하는 수밖에 없었다.

**수정**:
- `frontend/src/utils/imageUpload.js` 신규 — 검증 규칙과 에러 문구를 컴포넌트 밖 순수 함수로 분리
  - `validateImageFile(file)` — **타입을 크기보다 먼저** 검사한다. PDF를 고른 사용자에게 "10MB 이하만 됩니다"라고 하면 압축이라는 엉뚱한 행동을 유도하게 된다. 크기 초과 시에는 한계값과 실제 크기를 함께 준다(`10MB 이하만 업로드할 수 있습니다. (선택한 파일: 23.4MB)`). 예외를 던지지 않고 **에러 문구를 값으로 반환**(정상이면 `null`)해 호출부가 try/catch 없이 분기한다
  - `uploadErrorMessage(err)` — 503(Azure 환경변수 미주입)/403(권한)/413(서버 크기 거부)을 서로 다른 문구로 구분. DEF-1이 "취소됨" 한 마디로만 보여 원인 파악이 늦어졌던 것과 같은 실수를 피하기 위함
- `AdminPage.jsx` — 파일 선택 → 검증 → 업로드 → 받은 URL을 `imageUrl` 칸에 자동 입력. 업로드 중에는 파일 입력과 저장 버튼을 잠가(`uploading`) URL이 비어 있는 채로 상품이 저장되는 것을 막고, 에러는 `role="alert"`로, 결과는 썸네일 미리보기로 표시. URL 직접 입력 방식도 그대로 남겨둠

**설계 판단 2가지**:
1. `MAX_IMAGE_SIZE_BYTES`를 백엔드 `spring.servlet.multipart.max-file-size`(10MB)와 **같은 값으로 맞추고 그 계약을 테스트로 못 박았다**. 프론트가 더 관대하면 사용자는 업로드를 다 기다린 뒤 413을 맞고, 더 엄격하면 서버가 받을 수 있는 파일을 괜히 막는다.
2. `await` 뒤의 `setForm`은 **함수형 업데이트**(`setForm(prev => ...)`)로 썼다. 업로드를 기다리는 동안 사용자가 상품명 등 다른 칸을 고칠 수 있는데, `{...form}`을 쓰면 오래된 스냅샷으로 덮어써서 그 입력이 사라진다.

**검증**: `imageUpload.test.js` 15개 신규(경계값 10MB 정확히/1바이트 초과, 타입-크기 동시 위반 시 순서, `file.type`이 빈 문자열인 경우, 에러 매핑 5종) → 프론트 전체 45개 통과. `npm run build` 통과.

**배포 후 실전 재검증 완료** (같은 날, 커밋 `8322007` 배포 — run `30732838198`, 3개 job 전부 success, `Verify deploy secrets` 통과):

- 번들 `index-DcWjj621.js` → **`index-BFdlxuOz.js`** 교체 확인. 새 번들에 `이미지 파일 업로드`·`이미지 파일만 업로드할 수`·`admin/upload` 문자열 모두 존재. `index.html` 응답 헤더 `cache-control: no-cache`도 유지됨(DEF-10 수정 생존 확인)

| ID | 시나리오 | 결과 |
|---|---|---|
| S8-2R-1 | `/admin` 상품 관리에 파일 input 노출 | ✅ `#product-image-file` 1개 + "이미지 파일 업로드" 라벨 1개 (`def5-admin-upload-ui.png`) |
| S8-2R-2 | 실제 이미지 업로드 → URL 자동입력 + 미리보기 | ✅ `https://kdtstorage2026.blob.core.windows.net/product-images/<uuid>_def5-test.png` 자동 입력, 미리보기 `complete=true, naturalWidth=1` (`def5-upload-success.png`, `def5-preview-recheck.png`) |
| S8-2R-3 | `.txt` 선택 시 프론트 차단 | ✅ "이미지 파일만 업로드할 수 있습니다. (JPG, PNG, WebP, GIF)" 표시 + **`/api/admin/upload` 요청 0건** (`def5-type-rejected.png`) |
| S8-2R-4 | 업로드 URL로 상품 등록 | ✅ `TEST-20260802-DEF5` 관리자 목록에 생성 |
| S8-2R-5 | 정리 | ✅ TEST 상품 삭제, 총 상품 18개 복귀 |

> 검증 방법 메모: 이 세션에서는 Playwright **MCP 서버가 연결되지 않았다**(`npx -y @playwright/mcp@latest`가 패키지 최초 다운로드 때문에 30초 연결 타임아웃 초과). npx 캐시를 데운 뒤에도 MCP 연결은 세션 시작 시점에 맺어지므로, MCP 대신 **npx 캐시의 `playwright-core` 1.61.1을 Node 스크립트에서 직접 import해 Chromium을 구동**하는 방식으로 검증했다(해당 버전이 요구하는 chromium 리비전 1228이 로컬에 이미 설치돼 있었음). 다음 세션에서 MCP를 쓰려면 캐시가 데워져 있으므로 정상 연결될 것이다.

> ⚠️ 첫 실행에서 S8-2R-2가 FAIL로 나왔는데, **검증 스크립트 쪽 경쟁 조건**이었다 — URL이 채워지자마자 `naturalWidth`를 읽어 이미지 로드 완료를 기다리지 않았다. `load` 이벤트를 명시적으로 기다리도록 고쳐 재실행하니 통과. 자동 검증에서 "렌더 확인"은 항상 로드 완료를 기다린 뒤에 해야 한다.

### DEF-7 상세 — "가짜 링크"를 진짜 링크로 (커밋 `461dbf2`)

**원인**: 화면상 링크처럼 보이는 요소들이 실제로는 `<span onClick={() => navigate('/shop')}>`이었다. 브라우저에게 span은 그냥 글자라서 **Tab으로 도달 불가 / Enter 무반응 / 우클릭 "새 탭에서 열기" 불가 / 스크린리더가 링크로 읽지 않음** — 마우스 사용자만 쓸 수 있는 UI였다. `<main>`도 15개 페이지 중 5개(Admin·Cart·Home·PaymentFail·PaymentSuccess)에 빠져 있었다.

**수정 — "이동이냐 동작이냐"로 갈랐다**:

| 하는 일 | 올바른 태그 | 판단 기준 |
|---|---|---|
| 다른 경로로 **이동** | `<a>` (`TextLink`) | URL이 바뀐다 |
| 같은 화면에서 **상태 변경** | `<button type="button">` | URL이 그대로다 |

- `components/TextLink.jsx` 신규 — `<Link>`에 `color:inherit`·`textDecoration:none` 초기화를 얹은 공통 컴포넌트. 교체할 때마다 밑줄·파란색 지우는 코드를 반복하지 않기 위해 한 곳에 모았다
- 링크로 바꾼 곳(11): Footer "둘러보기" 3개, HomePage 히어로/섹션 CTA·"전체 보기"·"이야기 읽기", StoryPage "이 농가의 상품 보기", CartPage "계속 쇼핑하기", AboutPage CTA, ProductDetailPage "쇼핑으로 돌아가기", OrderDetailPage 3개
- 버튼으로 바꾼 곳(4): CartPage 배송지 선택 카드, StoryPage "이야기 더 보기"·"돌아가기", ProductDetailPage "새로고침"
- `<main>` 5개 페이지 추가. CartPage는 최상위가 `<form>`이라 한 겹 감쌌다(모달은 본문이 아니므로 `<main>` 밖에 유지)
- 부수 정리: 이동이 전부 `<a>`로 바뀌면서 AboutPage·OrderDetailPage·StoryPage의 `useNavigate`가 불필요해져 제거

**ProductCard — 이번 작업에서 유일하게 정답이 갈린 지점**:

카드 전체를 `<a>`로 감싸는 게 가장 간단해 보이지만 **불가능**하다. 카드 안에 장바구니 `<button>`이 있는데, `<a>` 안의 `<button>`은 HTML 스펙 위반(`<a>`의 내용 모델이 interactive content를 금지)이라 브라우저·스크린리더 동작이 제각각이 된다.

선택한 방식은 **stretched link**다:
- `<article class="product-card">`(position:relative) 안에서 **상품명만 진짜 `<a>`**
- 그 링크의 `::after`를 `inset:0`으로 카드 전체에 펼쳐 투명한 클릭 판을 만든다 → 마우스로는 예전처럼 카드 아무 데나 눌러도 상세로 간다
- 장바구니 버튼은 `position:relative; z-index:1`로 그 판 위에 올려 자기 클릭을 지킨다
- 결과: 탭 정지가 **카드당 2개(상품명 → 장바구니)**로 깔끔하다. 이미지·텍스트를 각각 `<Link>`로 감싸는 대안은 같은 목적지 링크가 중복돼 20개 목록에서 탭 정지가 60개가 된다
- API 변경: `onOpen` 콜백 → `to` 경로 prop (진짜 `<a>`가 되려면 href가 필요하므로)

**검증 (18/18 통과)** — 로컬 빌드를 `vite preview`로 띄우고 `/api`만 배포 백엔드로 넘겨 실측:
- `<main>` 각 페이지 정확히 1개
- 푸터 3개 항목이 `role=link` + 올바른 href, 히어로 CTA가 실제 Tab 경로에 등장
- 상품 카드 10개 전부: `<a>` 1개씩 / 장바구니 버튼이 `<a>` 안에 없음 / href가 `/products/`
- **`document.elementFromPoint`로 이미지 정중앙을 찍었더니 `<a class="product-card-link">`가 잡힘** — stretched link가 실제로 덮고 있음을 좌표로 확증
- StoryPage "이야기 더 보기"에 포커스 후 **Enter로 실제 동작** 확인
- `cursor:pointer`인데 `<a>`/`<button>` 밖이고 포커스도 못 받는 "유령 클릭 요소" 잔존 0건

> 검증 중 Playwright가 이미지 클릭을 거부하며 "링크가 pointer event를 가로챈다"고 보고했는데, 이게 **실패가 아니라 stretched link가 의도대로 동작한다는 증거**였다. 판정을 `elementFromPoint` + `force:true` 클릭으로 바꿔 확인했다.

**회귀 방지**: `ProductCard.test.jsx`에 접근성 테스트 3개 추가 — ① 상품명이 `role=link`이고 href가 맞는가 ② 카드 안 링크가 정확히 1개인가 ③ 장바구니 버튼의 `closest('a')`가 null인가. 누가 다시 `div onClick`으로 되돌리면 ①에서 바로 실패한다. 프론트 테스트 45 → **48개**.

> ⚠️ **남은 작업 → DEF-12로 등록** (2026-08-02, 다음 세션에서 처리)
>
> 커밋 후 전수 확인(`grep "onClick={() => navigate("`)에서 **한 곳이 빠진 것을 발견**했다 —
> `ProductDetailPage.jsx:384`의 추천 상품 카드 4개가 아직 `<div onClick>`이다.
>
> **왜 빠졌나:** 이 카드들만 `ProductCard` 컴포넌트를 쓰지 않고 페이지 안에 직접 복제해 그려놨다.
> 컴포넌트를 고치는 작업이라 **컴포넌트를 안 쓰는 복제본은 자동으로 빠졌다.**
> 회귀 테스트 3개도 `ProductCard.test.jsx`에 걸어둔 것이라 이 복제본은 지켜주지 못한다.
>
> **수정 방향(이미 재료는 다 있음):** 추천 카드를 `ProductCard`로 교체한다.
> `index.css`의 stretched link 규칙(`.product-card` / `.product-card-link` / `.product-card-action`)을
> 그대로 재사용하면 되고, `to={\`/products/${r.id}\`}`만 넘기면 된다.
> 다만 이 그리드는 4열 축약형(배지·별점 없음)이라 `featured` 옵션과 맞는지 먼저 확인할 것.
>
> **부수 효과:** 교체하면 복제본이 사라져 회귀 테스트의 보호 범위 안으로 들어온다.
> 즉 DEF-12는 접근성 수정인 동시에 **중복 제거**다.
>
> 참고로 `PaymentFailPage.jsx:18`의 `onClick={() => navigate(...)}`는 **`<button>`에 달려 있어 문제없다**
> (버튼은 Tab·Enter가 기본 동작). 다만 하는 일이 "이동"이라 엄밀히는 링크가 더 맞다 — 급하지 않음.

### DEF-12 상세 — 복제본은 컴포넌트 수정도, 회귀 테스트도 못 받는다 (커밋 `db5f396`)

**증상**: `/products/{id}` 하단 "이런 상품은 어떠세요?" 카드가 `<div onClick={() => navigate(...)}>`. DEF-7과 증상이 같다 — Tab 도달 불가 / Enter 무반응 / 새 탭 열기 불가 / 스크린리더가 링크로 읽지 않음.

**왜 DEF-7에서 안 잡혔나** (이게 이 결함의 교훈):
1. DEF-7의 수정 대상은 **`ProductCard` 컴포넌트**였다. 그런데 이 카드들만 컴포넌트를 쓰지 않고 페이지 안에 손으로 복제해뒀다 → **컴포넌트를 고쳐도 복제본은 안 고쳐진다.**
2. 회귀 테스트 3개도 `ProductCard.test.jsx`에 걸어뒀다 → **컴포넌트 테스트는 그 컴포넌트를 쓰는 곳만 지킨다.** 복제본은 보호 범위 밖이었다.

그래서 이번 회귀 테스트는 `ProductDetailPage.test.jsx`로 **페이지 단위**에 걸었다. "이 페이지가 실제로 링크를 렌더하는가"를 묻는 테스트라야 복제본이 되살아나도 잡힌다.

**수정**: 추천 카드를 `<ProductCard featured />`로 교체.
- `featured` 모드를 고른 이유: **홈 베스트 섹션이 이미 똑같은 4열 그리드에 이 모드를 쓴다**(배지·별점·장바구니 없는 축약형). 축약 카드의 정본이 이미 있으므로 새 변종(`compact` 등)을 만들지 않았다
- 부수 효과: 복제본이 사라져 `ProductCard.test.jsx`의 보호 범위 안으로 들어왔다. 즉 이 수정은 접근성 복구 **이자** 중복 제거다
- `index.css`의 stretched link 규칙(`.product-card` / `.product-card-link`)을 그대로 재사용 — 새 CSS 불필요

**같은 자리에서 함께 발견·수정한 것 — DEF-4의 복제본**:

로컬 빌드를 스크린샷으로 확인하다가, 추천이 2개인데 그리드가 `repeat(4,1fr)`로 고정돼 **빈 칸 2개가 회색 박스로 보이는 것**을 발견했다. 홈 베스트 섹션에서 이미 고친 DEF-4와 **똑같은 원인이고, 역시 복제본이라 그 수정을 못 받은 것**이다(이 결함은 내 변경 이전부터 있었다).

원인은 열 수가 아니라 이 그리드가 **컨테이너 배경(테두리색) + `gap: 1px`으로 경계선을 그리는 트릭**을 쓴다는 점이다 → 카드가 덮지 않은 영역은 그대로 회색으로 드러난다.

| 시도 | 결과 |
|---|---|
| 열 수를 카드 수에 맞춤 (`repeat(2,1fr)`) | 회색은 사라지지만 카드가 절반 폭으로 커져 **부가 정보인 추천 섹션이 화면을 압도** (이미지 높이 ~800px) |
| **열 수 + 컨테이너 폭을 함께 개수에 비례** (`maxWidth: 개수 × 25%`) | 회색 없음 + 카드 폭이 4열 기준으로 유지 → 추천이 2개든 4개든 카드 모양이 같다 ✅ |

모바일은 `.mobile-2col`로 2열로 접히므로 그 폭 제한을 풀어야 한다 → `index.css`에 `.rec-grid { max-width: 100% !important }` 추가. 안 풀면 화면의 50% 안에 2열이 들어가 카드가 4분의 1 폭이 된다.

**검증 (9/9 통과)** — 로컬 빌드를 `vite preview`로 띄우고 `/api`만 배포 백엔드로 넘겨 실측. DEF-7 검증에서 쓴 방식을 추천 섹션에 그대로 적용했다:
- 카드 2개 / 링크 2개, href가 `/products/3`·`/products/7`, **카드당 링크 1개**(탭 정지 폭발 없음)
- `<a>` 안의 `<button>` 없음(HTML 스펙 위반 방지), 유령 클릭 요소 0건
- **`document.elementFromPoint`로 이미지 정중앙을 찍어 `<a class="product-card-link">` 확인** — stretched link가 실제로 덮고 있음
- 링크에 포커스 → **Enter로 실제 이동 성공**(div였을 때는 포커스 자체가 불가능했다)
- 390px 가로 오버플로 없음, 데스크톱·모바일 스크린샷으로 회색 박스 제거 확인

> **계측 스크립트가 헛실패한 사례**: `elementFromPoint`가 처음에 `undefined`를 반환했다. 이 API는 **뷰포트 좌표 기준**이라 화면 밖 요소를 찍으면 null이 나오는데, 추천 섹션이 스크롤 아래에 있었기 때문이다. `scrollIntoView` 후 다시 재니 정상. DEF-9의 "헛통과"와 짝을 이루는 반대 사례다 — **계측이 실패하면 코드가 아니라 계측을 먼저 의심할 값도 있다.**

**회귀 방지**: `ProductDetailPage.test.jsx` 신규 5개 — ① 추천 카드가 `role=link`이고 href가 맞는가 ② 개수만큼 링크가 생기는가 ③ 링크가 `.product-card-link`이고 조상이 `.product-card`인가(stretched link 계약) ④ 그리드 열 수가 추천 개수를 따라가는가(회색 박스 방지) ⑤ 유령 클릭 요소 0건. 프론트 테스트 58 → **63개**.

**배포 후 실전 재검증 (CI run `30746207878`, 3개 job 전부 success, Deploy to Azure 3m0s)**:
- 번들 교체 확인: `index-BZFljAtE.js` → **`index-B7rJfUrY.js`**
- 배포 사이트에서 같은 스크립트 **9/9 통과** (로컬 빌드와 동일). 카드 2개·링크 2개, `elementFromPoint`로 `<a class="product-card-link">` 확인, Enter 이동 성공, 390px 오버플로 없음
- 증거: `def12-deployed-desktop.png` / `def12-deployed-mobile.png` — 저장소 루트, `.gitignore`의 `/*.png`로 미추적

**전수 확인**: `grep "onClick={() => navigate("`로 남은 가짜 링크를 다시 훑었다. 남은 것은 `PaymentFailPage.jsx:18` 한 곳뿐이고 **`<button>`에 달려 있어 Tab·Enter가 정상 동작하므로 접근성 문제는 없다**(하는 일이 "이동"이라 엄밀히는 링크가 더 맞지만 급하지 않음 — 이 판단은 DEF-7 때와 같다).

### DEF-9 상세 — 서버가 알려준 "끝"을 프론트가 버리고 있었다 (커밋 `c4c8143`)

**증상**: `/admin` → 주문 관리에서 주문이 1페이지 분량뿐인데도 "다음" 버튼이 활성 상태. 누르면 빈 표가 뜨고, **그제서야** 버튼이 잠긴다.

**재현 계측** (Playwright + API 응답 가로채기, 배포 사이트 구 번들 `index-DYdgOfmj.js`) — 화면 상태와 서버가 준 값을 나란히 대조했다:

| 대상 | 서버 응답 | 화면 "다음" | 판정 |
|---|---|---|---|
| 주문 탭 (5건) | `totalPages:1`, `last:true` | `disabled=false` | ❌ 불일치 |
| 상품 탭 2/2 페이지 | `totalPages:2`, `last:true` | `disabled=false` | ❌ 불일치 |
| 상품 탭 1/2 페이지 | `totalPages:2`, `last:false` | `disabled=false` | ✅ 일치 |

**1회차 기록의 정정**: §9에는 "AdminPage 주문 탭 고유 문제"로 적었지만, 실측해보니 **상품 탭도 똑같이 깨져 있었다.** 잘못된 조건이 두 패널에 복붙돼 있었기 때문이다(DEF-8의 "초기화" 버튼과 같은 구조 — 복사된 마크업은 버그도 복사한다).

**근본 원인**: 비활성화 조건이 `disabled={orders.length === 0}`이었다.

이건 "**이미 빈 페이지에 도착했을 때**" 잠긴다는 뜻으로, 판정이 한 칸 늦다. 정작 필요한 값인 `totalPages`는 백엔드가 처음부터 주고 있었다 —

```
GET /api/products?size=1 →
  { content: [...], totalPages: 18, totalElements: 18, last: false, first: true, number: 0 }
```

그런데 프론트는 `setProducts(res.data.content)`로 **`content`만 꺼내고 나머지를 통째로 버렸다.** 끝을 알 방법을 스스로 없앤 뒤, 남은 정보(`length`)로 궁여지책을 쓴 것이 이 결함의 실체다.

**수정** (`AdminPage.jsx`):
- 두 패널이 복사해 쓰던 페이징 마크업을 `Pager` 컴포넌트 하나로 합쳤다 — 고칠 자리를 한 곳으로 줄여, 조건을 한 번 고치면 양쪽이 같이 낫는다
- `loadProducts`/`loadOrders`가 `res.data.totalPages`도 상태로 챙기게 했다
- 조건: `const isLastPage = page >= totalPages - 1`

`===`가 아니라 `>=`인 이유가 세 경우를 한 줄로 덮는다:

| 경우 | 값 | `>=` 결과 | `===`였다면 |
|---|---|---|---|
| 정상 경계 | totalPages 5, page 4 | 잠김 ✅ | 잠김 ✅ |
| 결과 0건 (Spring은 `totalPages:0`) | page 0, totalPages 0 → `0 >= -1` | 잠김 ✅ | `0 === -1` false → **빈 화면에서 버튼이 열림** ❌ |
| 필터로 페이지 수가 줄어듦 | page 3인데 totalPages 1 | 잠김 ✅ | `3 === 0` false → **범위 밖으로 이동 가능** ❌ |

초기값 `totalPages: 0`도 같은 식으로 "응답 전에는 잠김"이 된다. **모를 때는 잠그는 쪽이 안전하다** — 열어두면 존재하지 않는 페이지로 갈 수 있다.

**회귀 방지**: `AdminPage.test.jsx` 신규 6개(이 프로젝트 두 번째 페이지 단위 테스트). DEF-8과 달리 **jsdom으로 충분하다** — 픽셀이 아니라 버튼의 `disabled`라는 DOM 상태를 보기 때문이다. 마지막 페이지까지 클릭해 이동하는 시나리오, 결과 0건, 상품 탭까지 포함. 프론트 테스트 52 → **58개**.

**검증 스크립트가 헛통과한 사례 (기록해둘 만한 함정)**: "잠긴 다음을 눌러도 빈 페이지로 안 간다"를 클릭 직후 바로 읽었더니 **고장난 상태에서도 통과**했다. React 리렌더가 아직 안 끝나 이전 화면이 남아 있었기 때문이다. 클릭 후 2.5초 대기 + 표 행 수까지 함께 확인하도록 고쳐서 판별력을 되찾았다.

**배포 후 실전 재검증 (CI run `30745443868`, 3개 job 전부 success, Deploy to Azure 3m4s)**:
- 번들 교체 확인: `index-DYdgOfmj.js` → **`index-BZFljAtE.js`**
- 번들 내용으로 확증: `totalPages` 문자열이 **구 번들 0회 → 신 번들 5회**. 프론트가 이 값을 아예 안 쓰고 있었다는 사실 자체가 지문이 됐다
- 배포 사이트 관리자 로그인 후 실측 **6/6 통과** (수정 전 같은 스크립트 **3/6**)

| 검사 | 수정 전 | 수정 후 |
|---|---|---|
| 상품 1/2 페이지 "다음" 활성 | ✅ | ✅ |
| 상품 2/2 페이지 "다음" 잠김 | ❌ `disabled=false` | ✅ `disabled=true` |
| 상품 화면·서버(`last`) 일치 | ❌ | ✅ |
| 주문 "다음"이 서버 `last`와 일치 | ❌ `last:true`인데 열림 | ✅ 잠김 |
| 잠긴 "다음" 클릭 후 빈 표로 안 넘어감 | (판별력 없던 검사) | ✅ 페이지 1 유지, 5행 |

- 증거: `def9-products-before.png` / `def9-orders-before.png`(수정 전, 마지막 페이지인데 버튼 열림) / `def9-products-deployed.png` / `def9-orders-deployed.png` — 저장소 루트, `.gitignore`의 `/*.png`로 미추적

### DEF-8 상세 — 한글 버튼이 세로로 쪼개진 이유

**증상**: 390×844(iPhone 14)에서 `/shop`의 "검색" 버튼이 `검`/`색` 두 줄로 분리. 검색어를 넣으면 나타나는 "초기화"도 `초기`/`화`로 분리.

**계측으로 확인한 실제 값** (Playwright, 배포 사이트):

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| 검색 버튼 크기 | **66 × 66** | 66 × 44 |
| 텍스트 줄 수 | **2줄** | 1줄 |
| `white-space` | `normal` | `nowrap` |
| `flex-shrink` | `1` | `0` |
| 가로 오버플로 | 없음 | 없음 |

**근본 원인** — 두 가지가 겹쳤다:
1. flex 아이템의 기본값은 `flex-shrink: 1`이라 **공간이 모자라면 자기 콘텐츠보다 작아진다.** 어디까지 작아지는지는 `min-width: auto`(최소 콘텐츠 너비)가 정한다.
2. **한글은 글자 사이 어디서나 줄바꿈이 허용**되므로, 버튼의 최소 콘텐츠 너비가 `검` 한 글자다. 즉 CSS 입장에서는 "66px까지 줄여도 괜찮다"가 맞는 계산이었다. 영문 `Search`였다면 단어 중간을 못 끊어 이 버그가 안 났다.

즉 오버플로가 아니라 **찌그러짐**이 원인이다. 그래서 가로 스크롤은 처음부터 없었고, 눈으로만 보면 "왜 깨지지?" 싶었던 것.

**수정** (`ProductListPage.jsx`):
- 두 버튼에 `flexShrink: 0` + `whiteSpace: 'nowrap'` — 애초에 안 찌그러지게 하고, 찌그러져도 줄바꿈은 막는다 (같은 코드베이스의 `MyPage.jsx` "주소 검색" 버튼이 이미 쓰던 선례)
- 입력창에 `minWidth: 0` — 버튼이 안 줄어드는 대신 줄어드는 몫을 입력창이 떠맡는다
- 입력창 `flex-basis` **280px → 180px** — 줄바꿈 판정은 "줄어들기 전 기준 크기"로 하므로, 280 그대로면 글자 깨짐은 고쳐져도 **버튼이 통째로 아랫줄로 밀려나** 설계된 한 줄 배치가 무너진다. 넓은 화면은 `maxWidth: 360`이 잡아줘서 데스크톱 모습은 변화 없음
- form에 `flexWrap: 'wrap'` — 그래도 안 들어가는 극단적 폭에서 가로 스크롤 대신 줄을 바꾸도록

**검증** — 로컬 빌드를 `vite preview`로 띄우고 `/api`만 배포 백엔드로 넘겨 실측:

| 뷰포트 | 입력창 폭 | 버튼 줄 수 | 한 줄 배치 | 가로 오버플로 |
|---|---|---|---|---|
| 320px | 206px | 1줄 | 유지 | 없음 |
| 375px | 261px | 1줄 | 유지 | 없음 |
| 390px | 276px | 1줄 | 유지 | 없음 |
| 768px | 360px | 1줄 | 유지 | 없음 |
| 1280px | 360px | 1줄 | 유지 | 없음 |

추가로 **390px 전수 검사**를 돌렸다 — `/`, `/shop`, `/story`, `/about`, `/cart`, `/login`, `/signup`의 모든 `<button>`/`<a>` 중 "공백 없는 8자 이하 라벨인데 `Range.getClientRects()`가 2줄 이상"인 것을 찾는 방식. **위반 0건**으로, 같은 원인의 다른 자리는 없다.

**회귀 방지**: `ProductListPage.test.jsx` 신규 4개. 단 **jsdom에는 레이아웃 엔진이 없어 "2줄로 그려지는가"는 테스트할 수 없다** — 그건 위 Playwright 계측이 담당한다. 대신 그 현상을 만든 CSS 조건(`nowrap`/`flexShrink:0`/`minWidth:0`/`flexWrap:wrap`)을 계약으로 못박아, 누가 지우면 실패하게 했다. 프론트 테스트 48 → **52개**.

**배포 후 실전 재검증 (커밋 `f8c3019`, CI run `30739568432` 3개 job 전부 success)**:
- 배포 번들 `index-DYdgOfmj.js`로 교체 확인, 번들 안에 `nowrap` 존재
- 배포 사이트 390px 실측 **5/5 통과** — 검색 66×44 1줄 / 초기화 1줄, 입력창과 버튼 `top=284`로 같은 줄, 가로 오버플로 없음
- 배포 사이트 7개 페이지 390px 전수 검사 **위반 0건**
- 증거: `def8-before.png`(수정 전 `검/색`) / `def8-deployed.png`(배포 후 한 줄) — 저장소 루트, `.gitignore`의 `/*.png`로 미추적

### DEF-1 상세 — 근본 원인과 수정 방법

**근본 원인**: GitHub Secrets에 **`VITE_TOSS_CLIENT_KEY`가 존재하지 않는다.**

```bash
gh secret list --repo johnjoseph1990/KDT-ShoppingMallProject
# ACR_LOGIN_SERVER, ACR_PASSWORD, ACR_USERNAME,
# AZURE_CREDENTIALS, AZURE_STORAGE_CONNECTION_STRING, TOSS_SECRET_KEY
# → VITE_TOSS_CLIENT_KEY 없음
```

`.github/workflows/ci.yml`의 `--build-arg VITE_TOSS_CLIENT_KEY=${{ secrets.VITE_TOSS_CLIENT_KEY }}`는 없는 시크릿을 **빈 문자열로 조용히 치환**한다. 에러도 경고도 없이 빌드·배포가 성공하고 결제만 죽는다.

**확증**: 배포된 번들(`index-DjSvkt_H.js`, 835KB)에 `test_ck_*`/`live_ck_*` 패턴이 **0건**이고, "결제 키가 설정되지 않았습니다" 가드 문구만 존재.

**백엔드는 정상**(비대칭 상태):
```
az containerapp secret list --name backend -g mins-shopping-rg --query "[].name"
# azure-storage-conn, pg-password, toss-secret-key  ← 모두 존재
# TOSS_SECRET_KEY → secretRef: toss-secret-key 바인딩 확인
```

**수정 절차** (순서 중요):
1. `gh secret set VITE_TOSS_CLIENT_KEY --repo johnjoseph1990/KDT-ShoppingMallProject` — 토스 상점의 **테스트 클라이언트 키**(`test_ck_...`). 반드시 백엔드 `TOSS_SECRET_KEY`와 **같은 상점**의 키여야 함 (짝이 안 맞으면 결제창에서 "잘못된 클라이언트 키")
2. master에 push하여 **프론트엔드 재빌드**를 트리거. ⚠️ 빌드타임 주입이므로 **Container App 환경변수를 바꾸는 것으로는 해결되지 않는다**
3. 배포 후 §S0-4로 번들 해시가 바뀐 것을 확인한 뒤 §S4-1(가상계좌) 재실행

**재발 방지 (적용 완료, 커밋 `6dad732`)**: CI `deploy` job 맨 앞에 `Verify deploy secrets` 스텝을 추가했다. 실전 로그에서 `필수 배포 시크릿 확인 완료.` 출력 확인. 참고 형태:
```yaml
- name: Verify frontend build secrets
  run: |
    if [ -z "${{ secrets.VITE_TOSS_CLIENT_KEY }}" ]; then
      echo "::error::VITE_TOSS_CLIENT_KEY is empty — 결제 기능이 죽은 채 배포됩니다"
      exit 1
    fi
```
