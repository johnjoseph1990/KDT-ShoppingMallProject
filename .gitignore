# IDE
.idea/
*.iml
.vscode/

# Java / Spring (backend)
build/
out/
.gradle/
*.class

# Node / React (frontend)
node_modules/
dist/
*.local

# 환경 변수·비밀값 (DB 비밀번호 등 — 절대 커밋 금지)
.env
.env.*
!.env.example
application-secret.yml

# OS
Thumbs.db
.DS_Store

# 로컬 실행 로그 (서버 기동 시 생성)
*.log

# Claude Code 로컬 설정
# .claude/ 내용물은 무시하되(개인 설정·캐시), 팀이 공유할 에이전트 정의만 예외로 추적
.claude/*
!.claude/agents/
.agents/

# 학습 슬라이드 (발표 확정 시에만 생성, 평소엔 추적 제외)
docs/learning/slides/
